function exportF(args){
	let sites = require('sites')(args)
	let domains = ``
	for(var site of sites){
		if(site.certbot){
			domains += `-d ${site.server_name} `
		}
	}
	return domains
}

module.exports = exportF